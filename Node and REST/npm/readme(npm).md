<!-- npm - global command, comes with node
npm --version, checks the version of the npm

local dependency - dependencies that are needed or installed onlt for a particular project 
npm i <packageName>

global dependency - dependencies that can be used in any project
npm install -g <packageName>

package.json - manifest file (stores important information about project/package)
npm init -y (everything default) --> 